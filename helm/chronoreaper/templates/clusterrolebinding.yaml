apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "chronoreaper.fullname" . }}
  labels:
    {{- include "chronoreaper.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "chronoreaper.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "chronoreaper.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
