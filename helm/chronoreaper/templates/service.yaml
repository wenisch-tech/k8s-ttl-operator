apiVersion: v1
kind: Service
metadata:
  name: {{ include "chronoreaper.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chronoreaper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chronoreaper.selectorLabels" . | nindent 4 }}
