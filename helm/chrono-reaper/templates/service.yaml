apiVersion: v1
kind: Service
metadata:
  name: {{ include "chrono-reaper.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chrono-reaper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chrono-reaper.selectorLabels" . | nindent 4 }}
