apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "chrono-reaper.fullname" . }}
  labels:
    {{- include "chrono-reaper.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "chrono-reaper.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "chrono-reaper.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
